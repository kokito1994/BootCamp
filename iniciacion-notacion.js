// Notacion

// ;     Sirve para delimitar el final de una linea
//  const b = 5;
// b  + 5;

// .     Se utiliza en los objetos para acceder a los objetos
// movil.anchura

// []   Listas , arreglos o arrays
const ar = [1, 2, 3, 4];
ar[2];
console.log(ar[2]);
// ()   Funciones
function suma( a , b) {
        // se escribe la funcion
}

// {} Laves para objetos, funciones y estructuras de control
const movil = {
    anchura:5,
    altura:10,

}

if (true) {
    //todo lo que haya entre llaves

    const constante2 = "hola"
}

